import { TextColorMapPipe } from './text-color-map.pipe';

describe('TextColorMapPipe', () => {
  it('create an instance', () => {
    const pipe = new TextColorMapPipe();
    expect(pipe).toBeTruthy();
  });
});
