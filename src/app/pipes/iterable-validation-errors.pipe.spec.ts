import { IterableValidationErrorsPipe } from './iterable-validation-errors.pipe';

describe('IterableValidationErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new IterableValidationErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
